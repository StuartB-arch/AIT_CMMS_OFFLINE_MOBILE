# Python bytecode
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
.venv/
venv/
env/

# OS / editor artefacts
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs
*.log

# CSV backups generated by CSVManager (single rolling backup per file)
PM_MASTER_2026_CLEANED_backup.csv
MRO_STOCK_backup.csv

# Per-site configuration (each installation has its own paths/name)
site_config.json

# Per-site SQLite database
ait_cmms.db
